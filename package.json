{
  "name": "p2bot",
  "version": "2.0.0",
  "description": "POC: Create a slack app for product packaging UI",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "@slack/bolt": "^3.6.0"
  }
}
